import 'package:flutter/material.dart';

class BrowseAnimalsPage extends StatelessWidget {
  const BrowseAnimalsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Browse Animals'),
      ),
    );
  }
}
